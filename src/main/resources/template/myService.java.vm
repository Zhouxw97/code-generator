package ${package.Service};

    #set($domainName=$!{entity})
    #set($end=$domainName.length() - 2)
    #set($domainName=$domainName.substring(0,$end)+"DTO" )
import com.yonyou.cyx.framework.service.base.IBaseService;
import ${package.Entity}.${entity};
import ${cfg.packageDto}.${domainName};


#foreach($field in ${table.fields})
    #if(${field.keyFlag})
        #set($keyPropertyType=${field.propertyType})
    #end
#end
/**
 * $!{table.comment} 服务类
 *
 * @author ${author}
 * @since ${date}
 */
#if(${kotlin})
interface ${table.serviceName} :${superServiceClass}<${entity}>
#else
public interface ${table.serviceName} extends IBaseService<${domainName},${entity},${keyPropertyType}> {

}
#end
